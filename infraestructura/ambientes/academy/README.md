# Ambiente AWS Academy

Directorio Terraform principal del ambiente AWS Academy.

Comandos principales:

- terraform fmt -recursive
- terraform init
- terraform validate
- terraform plan

Las credenciales se configuran localmente fuera del repositorio en ~/.aws/credentials.
